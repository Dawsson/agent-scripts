---
summary: 'Release workflow — see the releasing skill for full details.'
read_when:
  - Preparing a release or editing release notes.
---

# Releasing

See the `releasing` skill for the full workflow (changelog curation, GitHub releases, npm publish).

The `1password` skill covers secure npm publish with OTP via tmux + `op`.
